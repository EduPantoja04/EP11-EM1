# Copiar a .env antes de levantar la solución:
#   copy .env.example .env
# La contraseña real vive solo en .env (no se versiona).

DB_HOST=database
DB_PORT=5432
DB_NAME=rrhh
DB_USER=rrhh_user
DB_PASSWORD=cambiar_esta_clave
